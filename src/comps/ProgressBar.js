import React from 'react';
import useStorage from '../hooks/useStorage';

const ProgressBar = () =>{
    return(
        <div >
            progress
        </div>
    )
}

export default ProgressBar;